import Nerf from './nerfchars.js';

export default class Magician extends Nerf {
  constructor(name, type, distance) {
    super(name, 'Magician', distance);
    // this.attack = 10;
    // this.defense = 40;
  }
}
